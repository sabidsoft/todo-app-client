import React from 'react';

const Loading = () => {
    return (
        <div>
            <p>Loading...</p>
        </div>
    );
};

export default Loading;

// h-screen flex justify-center items-center